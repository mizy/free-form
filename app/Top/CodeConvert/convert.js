function object(obj){
    let each = ``;
    for(let key in obj){
        if(obj[key]){
            each+= `
                ${key}:${obj[key]}
            }`;
        }
    }
    let str = `{

    }`
}
export {

}